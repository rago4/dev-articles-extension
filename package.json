{
  "name": "dev-articles-extension",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build-extension": "yarn build && cp src/manifest.json dist/",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "10.5.15"
  },
  "devDependencies": {
    "@preact/preset-vite": "2.1.5",
    "typescript": "4.4.4",
    "vite": "2.7.2"
  }
}
